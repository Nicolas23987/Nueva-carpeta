{
  "name": "aplicaciones-para-el-servidor-web",
  "version": "1.0.0",
  "description": "\"# Aplicaciones-para-el-servidor-web\"",
  "main": "index.js",
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts", 
    "build": "rimraf ./dist && tsc", 
    "start": "npm run build && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  }
}
